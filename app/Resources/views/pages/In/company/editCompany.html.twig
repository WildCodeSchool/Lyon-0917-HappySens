{% extends 'baseIn.html.twig' %}

{% block content %}
    <div class="container">
        <div class="row">
            <img src="{{ asset('uploads/photoCompany/' ~ company.logo) }}" alt="">
            <h1>Company edit</h1>
        </div>
        {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
        {{ form_end(edit_form) }}
    </div>
{% endblock %}
