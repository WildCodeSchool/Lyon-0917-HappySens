{% extends 'baseIn.html.twig' %}

{% block body %}
    <h1>Company edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('listingCompany') }}">Back to the list</a>
        </li>
        <li>
            {#{{ form_start(delete_form_company) }}#}
                {#<input type="submit" value="Delete">#}
            {#{{ form_end(delete_form_company) }}#}
        </li>
    </ul>
{% endblock %}
